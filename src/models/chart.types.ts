export enum ChartType {
  LT_RATINGS = "ratings",
  SCORES = "scores",
}

export enum TableViewType {
  OVERVIEW = "overview",
  ESG = "esg",
}

export enum RatingChartType {
  LT_RATINGS = "ltRatings",
  BANK_LT_RATINGS = "bankLtRatings",
}
